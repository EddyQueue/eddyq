{
  "name": "@eddyq/nestjs",
  "version": "0.0.1",
  "description": "NestJS module for eddyq — decorators and DI for the eddyq job queue",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EddyQueue/eddyq.git",
    "directory": "packages/nestjs"
  },
  "homepage": "https://github.com/EddyQueue/eddyq",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests yet\" && exit 0",
    "lint": "echo \"no lint yet\" && exit 0",
    "docs": "typedoc"
  },
  "peerDependencies": {
    "@eddyq/queue": "workspace:*",
    "@nestjs/common": "^10 || ^11",
    "@nestjs/core": "^10 || ^11"
  },
  "devDependencies": {
    "@types/node": "^20",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  }
}
