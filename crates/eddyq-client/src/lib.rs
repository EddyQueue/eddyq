//! eddyq-client — enqueue and admin API for eddyq.
//!
//! Used by the CLI, the NAPI bindings, and any Rust application that wants to
//! enqueue jobs without running a worker.

#![forbid(unsafe_code)]
