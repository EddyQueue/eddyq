# eddyq-napi

NAPI-RS bindings that expose `eddyq-client` to Node.js. Not published to crates.io — consumed via the [`@eddyq/queue`](../../packages/queue) npm package.

Phase 3 deliverable; this crate currently exposes only `version()` as a smoke-test of the NAPI build pipeline.
