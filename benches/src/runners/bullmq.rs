//! Runner for `BullMQ` (Node, Redis). Shells out to a Node subprocess (like graphile),
//! and runs Redis alongside Postgres in the bench docker-compose.

// Stub: subprocess shim lives in benches/bullmq-shim/ (Node package, Phase 0 work item).
