//! Runner for graphile-worker (Node, Postgres). Shells out to a Node subprocess
//! that runs the actual worker; the Rust harness enqueues and measures.

// Stub: subprocess shim lives in benches/graphile-shim/ (Node package, Phase 0 work item).
