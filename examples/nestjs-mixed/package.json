{
  "name": "@eddyq/example-nestjs-mixed",
  "version": "0.0.0",
  "private": true,
  "description": "NestJS app with webhooks on Redis and payments on Postgres in a single process.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node --enable-source-maps dist/main.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@eddyq/nestjs": "workspace:*",
    "@eddyq/queue": "workspace:*",
    "@eddyq/wakeboard": "workspace:*",
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
